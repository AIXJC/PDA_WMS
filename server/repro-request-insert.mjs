import { pool } from './db.js';\nasync function run() {\n  const parsedTypeId = 3;\n  const normalizedPartNumber = 'BL-322';\n  const parsedQty = 1;\n  const parsedRegUserId = 1;\n  const normalizedSourceLocationId = 129;\n  const normalizedDestinationLocationId = null;\n  const lotReceiveId = 187;\n  const [insertResult] = await pool.query(\n    INSERT INTO INVENTORY_REQUESTS (\n      RequestStatusID, RequestTypeID, PartNumber, Quantity,\n      RegUserID, ConfirmUserID, SourceLocationID, DestinationLocationID, LotReceiveID\n    ) VALUES (40, ?, ?, ?, ?, ?, ?, ?, ?) , [\n      parsedTypeId,\n      normalizedPartNumber,\n      parsedQty,\n      parsedRegUserId,\n      parsedRegUserId,\n      normalizedSourceLocationId,\n      normalizedDestinationLocationId,\n      lotReceiveId,\n    ]);\n  console.log('insertResult', insertResult);\n  await pool.end();\n}\nrun().catch(err => { console.error(err); process.exit(1); });
